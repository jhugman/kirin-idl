var _ = require('lodash');

function IdlTranslator (options) {
  this._platform = options.platform;
  this._debug = options.debug;
  this._targetDirectory = options.targetDir;
}

_.extend(IdlTranslator.prototype, {
  
});

var functions = {

};

module.exports = {

};